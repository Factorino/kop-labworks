"""Domain layer: entities, value objects and business rules.

The innermost layer. It imports nothing from the rest of the package and
nothing from the outside world — no frameworks, no drivers, no I/O. That is
what makes it testable without any infrastructure at all.
"""
